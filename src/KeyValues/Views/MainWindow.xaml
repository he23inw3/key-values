<Window x:Class="KeyValues.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:components="clr-namespace:KeyValues.Views.Components"
        mc:Ignorable="d"
        Title="KeyValues - ローカルアカウント管理" 
        Height="650" Width="1000"
        MinHeight="550" MinWidth="850"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}"
        FontFamily="{StaticResource MainFont}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Top Toolbar -->
            <RowDefinition Height="*" />    <!-- Main Body -->
            <RowDefinition Height="Auto" /> <!-- Status Bar -->
        </Grid.RowDefinitions>

        <!-- Top Toolbar component -->
        <components:TopToolbar Grid.Row="0" />

        <!-- Main Body -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="340" />    <!-- Left Sidebar (Search, List) -->
                <ColumnDefinition Width="1" />      <!-- Splitter Border -->
                <ColumnDefinition Width="*" />      <!-- Right Details Pane -->
            </Grid.ColumnDefinitions>

            <!-- Left Sidebar component -->
            <components:AccountListPanel Grid.Column="0" DataContext="{Binding ListViewModel}" />

            <!-- Splitter Border -->
            <Border Grid.Column="1" Background="{DynamicResource BorderBrush}" Width="1"/>

            <!-- Right Details Pane component -->
            <components:AccountDetailPanel Grid.Column="2" DataContext="{Binding DetailViewModel}" />
        </Grid>

        <!-- Status Bar with Toast Notification -->
        <Border Grid.Row="2" 
                Background="{DynamicResource CardBackgroundBrush}" 
                BorderBrush="{DynamicResource BorderBrush}" 
                BorderThickness="0,1,0,0" 
                Padding="16,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <!-- Status Toast -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" x:Name="StatusContainer">
                    <!-- Dynamic check / warning icon depending on StatusType -->
                    <TextBlock FontFamily="{StaticResource FluentIcons}" 
                               FontSize="12" 
                               VerticalAlignment="Center" 
                               Margin="0,0,6,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="&#xE946;" /> <!-- Info i -->
                                <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding StatusType}" Value="Success">
                                        <Setter Property="Text" Value="&#xE73E;" /> <!-- Check -->
                                        <Setter Property="Foreground" Value="{DynamicResource SuccessBrush}" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding StatusType}" Value="Error">
                                        <Setter Property="Text" Value="&#xE783;" /> <!-- Alert / Warning -->
                                        <Setter Property="Foreground" Value="{DynamicResource DangerBrush}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <TextBlock Text="{Binding StatusMessage}" 
                               FontSize="12" 
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"/>
                </StackPanel>

                <!-- Offline notice -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Opacity="0.7">
                    <TextBlock FontFamily="{StaticResource FluentIcons}" 
                               Text="&#xF384;" 
                               FontSize="11" 
                               Foreground="{DynamicResource SuccessBrush}" 
                               VerticalAlignment="Center" 
                               Margin="0,0,4,0"/>
                    <TextBlock Text="ローカル暗号化保護中 (完全オフライン)" FontSize="10" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
